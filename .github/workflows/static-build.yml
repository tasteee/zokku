name: Static build

on:
  push:
    branches:
      - feature/local-workspace
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install dependencies
        run: npm install

      - name: Build static bundle
        run: npm run build

      - name: Verify build output
        run: test -f out/index.html && test -f out/404.html && test -d out/assets
